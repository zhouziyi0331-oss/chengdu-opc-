# Nginx配置文件 - 成都OPC中心后端API
# 文件位置: /etc/nginx/sites-available/chengdu-opc

server {
    listen 80;
    server_name api.成都opc.top;

    # 日志文件
    access_log /var/log/nginx/chengdu-opc-access.log;
    error_log /var/log/nginx/chengdu-opc-error.log;

    # 客户端最大上传大小
    client_max_body_size 10M;

    # 反向代理到Node.js后端
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;

        # WebSocket支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';

        # 传递真实客户端信息
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 缓存设置
        proxy_cache_bypass $http_upgrade;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 健康检查端点（不记录日志）
    location /api/health {
        proxy_pass http://localhost:3000;
        access_log off;
    }

    # 静态文件缓存（如果有）
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://localhost:3000;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS配置（运行 certbot 后会自动添加）
# server {
#     listen 443 ssl http2;
#     server_name api.成都opc.top;
#
#     ssl_certificate /etc/letsencrypt/live/api.成都opc.top/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.成都opc.top/privkey.pem;
#
#     # SSL优化配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # 其他配置同上...
# }
